/**
 * 栈是一个运算受限的线性表
 * 只允许在栈一端进行插入和删除操作
 * ! 特点： 先进后出， 后进先出
 * 程序中的栈结构：
 *  1. 函数调用栈
 *  2. 递归 
 */

/**
 * 栈的常见操作：
 *  1. push():添加元素
 *  2. pop(): 出栈
 *  3. peek(): 返回栈顶的元素，不会对栈做任何修改
 *  4. isEmpty()：如果栈中没有元素就返回true, 否则返回false
 *  5. size(): 返回栈里的元素个数，
 *  6. toString(): 将栈结构的内容以字符串的形式返回
 */